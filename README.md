# ogx-service-identity

```bash
dotnet nuget add source "https://nuget.pkg.github.com/hsn-soft/index.json" \
    --name github \
    --username hsnsh \
    --password <GITHUB_TOKENIN> \
    --store-password-in-clear-text
```
